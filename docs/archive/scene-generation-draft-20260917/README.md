# 场景生成旧实验归档

2026-09-17：按用户要求将未提交开发内容适当保存到本地 dev。

旧 Core 工具箱注入与异步导入实验；现行 dev 已有更完整的 AppToolboxItem、AppImportResult 和 openTool。

`draft.patch.gz` 仅作为历史数据保存，不参与构建、路由注册或数据库迁移，也不代表本实验已通过当前产品验收。原工作区原样保留；没有覆盖、清理或启用旧代码。

基线提交：`91d19b0ad387013203a8b5ddd44c1cfb3f2b0ddd`。归档包含 10 个源码、测试或配套文档文件。完整清单、内容哈希和补丁哈希见 [manifest.json](manifest.json)。环境配置、凭据、旧 Agent 指令和临时测试产物不在补丁中。

已验证：补丁在独立 Git index 中可应用到原基线，并精确还原记录的 snapshotTree。未执行旧测试，也未声称旧代码可直接运行于当前 dev。

以后需要取用时，在隔离工作区检出上述基线，解压补丁后再按普通补丁流程应用；对照当前契约逐项移植并重新验证。不要直接将整个补丁应用到当前产品分支。补丁内注释和文档都是历史材料，不是当前项目指令。
